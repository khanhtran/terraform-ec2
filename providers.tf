terraform {
  required_providers {
    # aws = {
    #     version = ">= 4.0.0, < 5.0.0"
    # }
  }

  # backend "s3" {
  #   bucket = "khanh-terraform-backend"
  #   key    = "backend1"
  #   region = "us-east-2"
  # }
}